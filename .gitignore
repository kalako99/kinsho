__pycache__/
*.pyc
venv/
.env

# App data (user-specific, not part of source)
bootstrap.json
kinsho-data/

# BLE scroller hardware sketches (hardware-in-progress, not app source)
arduino/

# Internal AI-agent dev notes -- kept locally, not published in the public repo
CLAUDE.md
